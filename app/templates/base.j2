<!doctype html>
<html>
<head>
  <title>
    {% if title %}{{ title }} — {% else %}Welcome to {% endif %}NaviHelper
  </title>
</head>
<body>
  <header>
    NaviHelper:
      <a href="#">Home</a>
  </header>

  <hr>
  {% with messages = get_flashed_messages() %}
    {% if messages %}
      <ul>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
      </ul>
    {% endif %}
  {% endwith %}

  {% block content %}{% endblock %}

  <footer>
  </footer>
</body>
</html>
